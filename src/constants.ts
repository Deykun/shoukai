export const PATHS_DATA: {
  type?: string,
  pathNameLink: string,
  path: string,
  title: string,
  social: string,
  lang: string,
}[] = [
] as const;

export const LOCAL_STORAGE = {
  'SPES_USER_LANG': 'SPES_USER_LANG',
} as const;
